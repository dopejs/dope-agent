export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">DopeAgent</p>
        <h1>Operator Console Scaffold</h1>
        <p className="lead">
          The daemon will own system truth. This Web UI is a client surface that
          will consume daemon APIs and event streams.
        </p>
      </section>
    </main>
  );
}
