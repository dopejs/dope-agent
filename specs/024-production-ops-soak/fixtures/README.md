# Roadmap 39 Evidence Fixtures

Fixtures under this directory are non-production examples used by contracts and
runbooks. They use fake tenant ids, fake integration domains, secret references,
and redacted metadata only.

No fixture may contain raw credential values, access tokens, refresh tokens,
OAuth codes, provider tokens, local CLI auth material, or production tenant ids.
