# Capability Processes

This directory holds supervised external capability processes.

These are outside the daemon but managed by it.

Use this boundary for features that are:

- heavy
- fragile
- dependency-rich
- safer when isolated
