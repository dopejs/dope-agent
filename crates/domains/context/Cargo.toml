[package]
name = "dope-context"
description = "Context assembly policies: deterministic memory-bootstrap injection for the chat/pre-dispatch hook"
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
