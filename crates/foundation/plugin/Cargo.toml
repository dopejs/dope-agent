[package]
name = "dope-plugin"
description = "Plugin kernel: descriptors, profile-driven assembly resolution, seam registry, hook bus"
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[dependencies]
parking_lot = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
