// Roadmap 41 evaluation product support extends the replay harness with
// tenant-scoped discovery policies, bounded discovery runs, product-managed
// fixtures, replay campaigns, dashboard projections, tool-call inspection,
// retention/deletion handling, and audit-visible redaction failures.
package evaluation
