# Integration Diagnostic Contract Fixtures

Fixtures in this directory cover Roadmap 42 integration diagnostic API, event, smoke,
audit, retention, and release-readiness contracts.

Payloads must remain tenant-scoped and redacted. They must not contain raw secrets,
OAuth tokens, refresh tokens, app secrets, authorization headers, local CLI auth
material, or credential-bearing provider request/response bodies.
