# Activation Contract Fixtures

This directory contains metadata-only activation contract fixtures used by
`daemon/internal/contracts` tests.

Fixtures must not contain raw secrets, tokens, authorization headers, provider payloads,
test chat query text, reply text, transcripts, streamed deltas, or prompts.
