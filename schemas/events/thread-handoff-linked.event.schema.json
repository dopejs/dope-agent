{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.kura.agent/events/thread-handoff-linked.event.schema.json",
  "title": "ThreadHandoffLinkedEvent",
  "type": "object",
  "additionalProperties": false,
  "required": ["eventKind", "resourceId", "status", "permissionGate", "redactionStatus", "occurredAt"],
  "properties": {
    "tenantId": { "type": "string", "minLength": 1 },
    "threadId": { "type": "string" },
    "sessionSegmentId": { "type": "string" },
    "eventKind": { "type": "string", "const": "thread.handoff_linked" },
    "resourceId": { "type": "string", "minLength": 1 },
    "status": { "type": "string", "enum": ["succeeded", "denied", "failed_closed", "unsupported", "expired"] },
    "reasonCode": { "type": "string" },
    "permissionGate": { "type": "string", "const": "connectors.manage" },
    "redactionStatus": { "type": "string", "enum": ["redacted", "suppressed", "redaction_failed"] },
    "occurredAt": { "type": "string", "format": "date-time" }
  }
}
