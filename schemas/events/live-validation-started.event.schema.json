{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kura.local/schemas/events/live-validation-started.event.schema.json",
  "title": "LiveValidationStartedEvent",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "type": "object",
      "properties": {
        "category": { "const": "evaluation" },
        "name": { "const": "live_validation.started" },
        "payload": { "$ref": "#/$defs/payload" }
      }
    }
  ],
  "$defs": {
    "payload": {
      "type": "object",
      "required": ["validationId", "tenantId", "candidateId", "environmentScope", "status"],
      "properties": {
        "validationId": { "type": "string" },
        "tenantId": { "type": "string" },
        "candidateId": { "type": "string" },
        "environmentScope": { "type": "string" },
        "status": { "type": "string", "enum": ["queued", "running", "awaiting_approval"] }
      }
    }
  }
}
