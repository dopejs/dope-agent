{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dope.local/schemas/events/llm-dispatch-failed.event.schema.json",
  "title": "LLMDispatchFailedEvent",
  "allOf": [
    {
      "$ref": "./runtime-event.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "category": {
          "const": "llm"
        },
        "name": {
          "const": "llm.dispatch.failed"
        },
        "payload": {
          "type": "object",
          "required": ["provider", "model", "status", "partial", "attemptCount", "finishReason", "usage", "errorCode", "error"],
          "additionalProperties": false,
          "properties": {
            "provider": { "type": "string" },
            "model": { "type": "string" },
            "status": { "type": "string", "enum": ["failed"] },
            "partial": { "type": "boolean", "const": false },
            "attemptCount": { "type": "integer", "minimum": 1 },
            "finishReason": { "type": "string" },
            "usage": {
              "type": "object",
              "required": ["inputTokens", "outputTokens", "totalTokens"],
              "additionalProperties": false,
              "properties": {
                "inputTokens": { "type": "integer", "minimum": 0 },
                "outputTokens": { "type": "integer", "minimum": 0 },
                "totalTokens": { "type": "integer", "minimum": 0 }
              }
            },
            "errorCode": { "type": "string" },
            "error": { "type": "string" },
            "skills": {
              "type": "array",
              "items": { "type": "string" }
            },
            "skillContracts": {
              "type": "array",
              "items": {
                "$ref": "../api/sandbox-consumer-view.schema.json"
              }
            }
          }
        }
      }
    }
  ]
}
