{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kura.local/schemas/events/reminder-workflow-launch-failed.event.schema.json",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "type": "object",
      "properties": {
        "category": { "const": "reminder" },
        "name": { "const": "reminder.workflow_launch_failed" },
        "resource": {
          "type": "object",
          "properties": {
            "kind": { "const": "reminder" }
          }
        },
        "payload": {
          "type": "object",
          "required": ["reminderId", "occurrenceId", "actionKind", "reason"]
        }
      }
    }
  ]
}
