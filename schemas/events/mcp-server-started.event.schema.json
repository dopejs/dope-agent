{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dope.local/schemas/events/mcp-server-started.event.schema.json",
  "title": "MCPServerStartedEvent",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "type": "object",
      "properties": {
        "category": { "const": "mcp" },
        "name": { "const": "mcp.server_started" },
        "resource": {
          "type": "object",
          "required": ["kind", "id"],
          "properties": {
            "kind": { "const": "mcp_server" },
            "id": { "type": "string" }
          }
        },
        "payload": {
          "type": "object",
          "required": ["serverId", "status", "executionId", "toolCount", "transportKind"],
          "additionalProperties": true,
          "properties": {
            "serverId": { "type": "string" },
            "status": { "type": "string" },
            "executionId": { "type": "string" },
            "toolCount": { "type": "integer", "minimum": 0 },
            "transportKind": { "type": "string" }
          }
        }
      }
    }
  ]
}
