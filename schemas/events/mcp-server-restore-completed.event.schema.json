{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dope.local/schemas/events/mcp-server-restore-completed.event.schema.json",
  "title": "MCPServerRestoreCompletedEvent",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "type": "object",
      "properties": {
        "category": { "const": "mcp" },
        "name": { "const": "mcp.server_restore_completed" },
        "resource": {
          "type": "object",
          "required": ["kind", "id"],
          "properties": {
            "kind": { "const": "mcp_server" },
            "id": { "type": "string" }
          }
        },
        "payload": {
          "type": "object",
          "required": ["serverId", "transportKind", "sessionId", "toolCount"],
          "additionalProperties": true,
          "properties": {
            "serverId": { "type": "string" },
            "transportKind": { "type": "string" },
            "sessionId": { "type": "string" },
            "toolCount": { "type": "integer", "minimum": 0 }
          }
        }
      }
    }
  ]
}
