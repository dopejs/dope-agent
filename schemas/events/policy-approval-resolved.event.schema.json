{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dope.local/schemas/events/policy-approval-resolved.event.schema.json",
  "title": "PolicyApprovalResolvedEvent",
  "type": "object",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "type": "object",
      "properties": {
        "category": { "const": "policy" },
        "name": { "const": "policy.approval_resolved" },
        "resource": {
          "type": "object",
          "required": ["kind", "id"],
          "additionalProperties": false,
          "properties": {
            "kind": { "const": "approval" },
            "id": { "type": "string" }
          }
        },
        "payload": {
          "type": "object",
          "required": ["action", "resourceKind", "resourceId", "status", "resolution"],
          "additionalProperties": false,
          "properties": {
            "action": { "type": "string" },
            "resourceKind": { "type": "string" },
            "resourceId": { "type": "string" },
            "status": { "type": "string", "enum": ["approved", "rejected"] },
            "resolution": { "type": "string", "enum": ["approved", "rejected"] },
            "reason": { "type": "string" },
            "comment": { "type": "string" },
            "sandbox": { "$ref": "../api/sandbox-consumer-view.schema.json" }
          }
        }
      }
    }
  ]
}
