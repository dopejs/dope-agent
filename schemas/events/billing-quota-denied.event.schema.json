{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kura.local/schemas/events/billing-quota-denied.event.schema.json",
  "title": "BillingQuotaDeniedEvent",
  "type": "object",
  "required": ["eventId", "sequence", "category", "name", "occurredAt", "scope", "resource", "payload"],
  "additionalProperties": true,
  "properties": {
    "eventId": { "type": "string" },
    "sequence": { "type": "integer" },
    "category": { "const": "billing" },
    "name": { "const": "billing.quota_denied" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "scope": { "type": "object" },
    "resource": { "type": "object" },
    "payload": { "type": "object", "required": ["tenantId", "category", "operationKey", "reasonCode", "requestedAmount", "remainingAmount"], "additionalProperties": true }
  }
}
