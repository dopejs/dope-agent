{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dope.local/schemas/events/connector-telegram-setup-validated.event.schema.json",
  "title": "ConnectorTelegramSetupValidatedEvent",
  "type": "object",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "properties": {
        "category": { "const": "connector" },
        "name": { "const": "connector.telegram_setup_validated" },
        "resource": {
          "type": "object",
          "required": ["kind", "id"],
          "additionalProperties": false,
          "properties": {
            "kind": { "const": "telegram_hosted_setup" },
            "id": { "type": "string" }
          }
        },
        "payload": {
          "type": "object",
          "required": [
            "tenantId",
            "connectorId",
            "terminalState",
            "hostedReady",
            "credentialState",
            "allowmentState",
            "reasonCode",
            "redactionStatus",
            "validatedAt"
          ],
          "additionalProperties": true,
          "properties": {
            "tenantId": { "type": "string" },
            "connectorId": { "type": "string" },
            "terminalState": {
              "type": "string",
              "enum": ["ready", "degraded", "unavailable", "cancelled", "action-required"]
            },
            "hostedReady": { "type": "boolean" },
            "credentialState": {
              "type": "string",
              "enum": ["missing", "submitted", "valid", "invalid", "revoked", "redaction_suppressed"]
            },
            "allowmentState": {
              "type": "string",
              "enum": ["none", "partial", "valid", "stale"]
            },
            "reasonCode": { "type": "string" },
            "redactionStatus": {
              "type": "string",
              "enum": ["redacted", "suppressed", "redaction_failed"]
            },
            "validatedAt": { "type": "string", "format": "date-time" }
          }
        }
      }
    }
  ]
}
