{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kura.local/schemas/events/mcp-catalog-lifecycle-failed.event.schema.json",
  "title": "MCPCatalogLifecycleFailedEvent",
  "allOf": [
    { "$ref": "./runtime-event.schema.json" },
    {
      "type": "object",
      "properties": {
        "category": { "const": "mcp" },
        "name": { "const": "mcp.catalog_lifecycle_failed" },
        "resource": {
          "type": "object",
          "required": ["kind", "id"],
          "properties": {
            "kind": { "const": "mcp_server" },
            "id": { "type": "string" }
          }
        },
        "payload": {
          "type": "object",
          "required": ["actionId", "action", "serverId", "catalogEntryId", "status", "failureClass", "reason", "environment"],
          "additionalProperties": false,
          "properties": {
            "actionId": { "type": "string" },
            "action": { "type": "string", "enum": ["refresh", "reinstall", "uninstall"] },
            "serverId": { "type": "string" },
            "catalogEntryId": { "type": "string" },
            "status": { "type": "string", "enum": ["blocked", "failed"] },
            "failureClass": { "type": "string" },
            "reason": { "type": "string" },
            "environment": { "type": "string", "enum": ["test", "prod"] }
          }
        }
      }
    }
  ]
}
